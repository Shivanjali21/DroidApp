<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/color_gray_solid</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/color_black</item>
    </style>
</resources>